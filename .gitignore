node_modules/
.env*
.dev.vars
.dev.vars.*
!.dev.vars.example
.wrangler/
.DS_Store
Thumbs.db
# Cron tick concurrency guard — runtime only, never commit
.agent-lock
# Scratch space used by autonomous ticks
.tmp/

# Dump basis data produksi / data sensitif
db/embun_laundry.sql
db/dhani_laundry.sql
